version: "3.9"

services:
  balancer:
    build:
      context: .
      dockerfile: ./cmd/balancer/Dockerfile
    volumes:
      - .:/src
    ports:
      - 9090:9090

  backend-1:
    build:
      context: .
      dockerfile: ./cmd/backend/Dockerfile
    environment:
      - SERVNAME=backend-1
    volumes:
      - .:/src
    ports:
      - 9091:9090
